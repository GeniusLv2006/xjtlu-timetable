services:
  app:
    image: ghcr.io/geniuslv2006/xjtlu-timetable:${IMAGE_TAG:-latest}
    pull_policy: always
    container_name: xjtlu-timetable
    restart: unless-stopped
    ports:
      - "172.17.0.1:8091:8080"
    volumes:
      - ./backend/pb_data:/pb/pb_data
